[tool.poetry]
name = "smart-home-security-advisor"
version = "0.1.0"
description = "An AI agent that provides home security advice based on crime data."
authors = ["Agent Foundry AI"]

[tool.poetry.dependencies]
python = "^3.8"
pydantic = "^2.9.0"
langchain-openai = "^0.2.0"
python-dotenv = "^1.2.1"
requests = "^2.25.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
